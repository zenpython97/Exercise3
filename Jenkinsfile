pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'uname -a'
                echo "Jenkinsfile in the testing branch"
            }
        }
    }
}